apiVersion: v1
kind: Service
metadata:
  name: {{ include "accounts_service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "accounts_service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
