apiVersion: v1
kind: Service
metadata:
  name: {{ include "exchange_service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "exchange_service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
