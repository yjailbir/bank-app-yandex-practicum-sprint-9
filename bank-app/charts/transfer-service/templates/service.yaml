apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfer_service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "transfer_service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
