spring:
  application:
    name: exchange-generator-service

jwt:
  validation:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always