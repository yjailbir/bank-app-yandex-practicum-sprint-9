apiVersion: v1
kind: Service
metadata:
  name: {{ include "ui_service.fullname" . }}
  labels:
    app: {{ include "ui_service.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ui_service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
